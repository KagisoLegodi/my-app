/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['next-ecommerce-api.vercel.app','cdn.dummyjson.com'], // Add your image domain here
      },
};

export default nextConfig;
